# Build artifacts
build/
.build/
daemon/target/

# VM runtime bundle (generated by createVM / demo.mjs)
bundle/

# Large disk images (not suitable for git)
*.img
*.img.orig
*.img.work

# Except: keep srt-settings.json (small config file in daemon/)
!daemon/srt-settings.json

# macOS
.DS_Store

# Node.js
node_modules/
